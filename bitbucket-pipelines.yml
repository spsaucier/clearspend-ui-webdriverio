image: yukinying/chrome-headless-browser-stable

pipelines:
  default:
    - parallel:
        - step:
            name: Build and Test
            caches:
              - node
            script:
              - npm install
              - npm run smoke
