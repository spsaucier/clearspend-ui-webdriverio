definitions:
  services:
    docker-custom:
      type: docker 
      image: yukinying/chrome-headless-browser

pipelines:
  default:
    - step:
        runs-on: 
          - 'self.hosted'
          - 'macos'
        script:
          - npm i
          - npm run smoke